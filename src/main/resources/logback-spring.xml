<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Conversores de color de Spring Boot -->
    <conversionRule conversionWord="clr"
        converterClass="org.springframework.boot.logging.logback.ColorConverter"/>
    <conversionRule conversionWord="wex"
        converterClass="org.springframework.boot.logging.logback.WhitespaceThrowableProxyConverter"/>

    <!-- Patrón: hora | nivel | logger | mensaje -->
    <property name="PATRON"
        value="%clr(%d{HH:mm:ss.SSS}){faint} %clr(%-5level) %clr(---){faint} %clr(%-40.40logger{36}){cyan} : %m%n%wex"/>

    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>${PATRON}</pattern>
            <charset>UTF-8</charset>
        </encoder>
    </appender>

    <!-- Nuestro código: DEBUG para ver todo el detalle -->
    <logger name="com.pasajeya" level="DEBUG" additivity="false">
        <appender-ref ref="CONSOLE"/>
    </logger>

    <!-- Spring Web: INFO para ver arranque y mapeo de rutas -->
    <logger name="org.springframework.web" level="INFO"/>

    <!-- Resto de Spring y librerías: solo errores -->
    <logger name="org.springframework" level="WARN"/>

    <root level="INFO">
        <appender-ref ref="CONSOLE"/>
    </root>

</configuration>
